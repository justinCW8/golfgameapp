// Secrets.swift.template — copy to Secrets.swift and fill in real values.
// Secrets.swift is gitignored and must never be committed.
enum Secrets {
    static let golfCourseAPIKey = "YOUR_GOLF_COURSE_API_KEY"
}
